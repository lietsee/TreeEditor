import React from 'react';
import ReactDOM from 'react-dom/client';
import TreeVisualEditor from '../index';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TreeVisualEditor />
  </React.StrictMode>
);